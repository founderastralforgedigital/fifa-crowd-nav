{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install fastapi 'uvicorn[standard]' pydantic pydantic-settings 'python-jose[cryptography]' 'passlib[bcrypt]' httpx 'redis[asyncio]' structlog python-multipart"
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
